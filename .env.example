# Copier vers .env et renseigner (valeurs publiques — la clé anon est
# embarquée dans le bundle, la sécurité repose sur la RLS Supabase).
VITE_SUPABASE_URL=https://<project-ref>.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=<anon-key>
